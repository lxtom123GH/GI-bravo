node_modules/
server.log
*.log

# Roast Lab capture drops — personal roast data / debug artifacts (keep the README)
roast-logs/*
!roast-logs/README.md

# Local secrets / Firebase
.env
.env.local
.secrets/
*service-account*.json
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log

# Build output — Vercel rebuilds from source; stop committing it to avoid drift/conflicts
dist/
